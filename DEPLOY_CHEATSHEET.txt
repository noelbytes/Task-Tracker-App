╔═══════════════════════════════════════════════════════════════╗
║                    🚀 DEPLOY TO RENDER.COM                    ║
║                   (Completely FREE - 10 Minutes)              ║
╚═══════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────┐
│ STEP 1: Push to GitHub (2 min)                               │
└───────────────────────────────────────────────────────────────┘

  $ git add .
  $ git commit -m "Ready for deployment"
  $ git push origin main

┌───────────────────────────────────────────────────────────────┐
│ STEP 2: Deploy Database (1 min)                              │
└───────────────────────────────────────────────────────────────┘

  1. Go to: https://dashboard.render.com
  2. New + → PostgreSQL → Free
  3. Name: tasktracker-db
  4. COPY the "Internal Database URL"

┌───────────────────────────────────────────────────────────────┐
│ STEP 3: Deploy Backend (3 min)                               │
└───────────────────────────────────────────────────────────────┘

  1. New + → Web Service
  2. Connect GitHub repo
  3. Root Directory: backend
  4. Environment: Docker
  5. Add Environment Variables:

     SPRING_PROFILES_ACTIVE = prod
     DATABASE_URL = jdbc:postgresql://<host>:5432/<database>
     DB_USERNAME = <database user>
     DB_PASSWORD = <database password>
     JWT_SECRET = <run: openssl rand -base64 64>
     CORS_ORIGINS = https://tasktracker-frontend.onrender.com

     Where to find values (Render DB page):
       Internal Database URL example:
         postgresql://user:pass@dpg-xxxx.<region>-postgres.render.com:5432/dbname
       Set variables as:
         DATABASE_URL = jdbc:postgresql://dpg-xxxx.<region>-postgres.render.com:5432/dbname
         DB_USERNAME  = user
         DB_PASSWORD  = pass

  6. Create Web Service (wait 5-10 min)
  7. COPY your backend URL

┌───────────────────────────────────────────────────────────────┐
│ STEP 4: Update Frontend Config (1 min)                       │
└───────────────────────────────────────────────────────────────┘

  Edit: frontend/src/environments/environment.prod.ts

  export const environment = {
    production: true,
    apiUrl: 'https://YOUR-BACKEND-URL.onrender.com/api'
  };

  $ git add frontend/src/environments/environment.prod.ts
  $ git commit -m "Update API URL"
  $ git push

┌───────────────────────────────────────────────────────────────┐
│ STEP 5: Deploy Frontend (3 min)                              │
└───────────────────────────────────────────────────────────────┘

  1. New + → Static Site
  2. Connect GitHub repo
  3. Root Directory: frontend
  4. Build Command: npm ci && npm run build
  5. Publish Directory: dist/frontend/browser
  6. Create Static Site (wait 3-5 min)

┌───────────────────────────────────────────────────────────────┐
│ STEP 6: Update CORS (1 min)                                  │
└───────────────────────────────────────────────────────────────┘

  1. Go to backend service
  2. Environment → CORS_ORIGINS
  3. Update to your actual frontend URL
  4. Save (backend will redeploy)

┌───────────────────────────────────────────────────────────────┐
│ STEP 7: TEST YOUR APP! 🎉                                    │
└───────────────────────────────────────────────────────────────┘

  Visit: https://tasktracker-frontend-XXXX.onrender.com
  Login: demo / demo123

  ✅ Your app is LIVE!

╔═══════════════════════════════════════════════════════════════╗
║                       ⚠️  IMPORTANT NOTES                      ║
╚═══════════════════════════════════════════════════════════════╝

• First load after sleep: 30-60 seconds (services wake up)
• After that: Fast and responsive
• Free database expires: 90 days (Render emails you)
• Cost: $0/month forever

╔═══════════════════════════════════════════════════════════════╗
║                      📚 DETAILED GUIDES                        ║
╚═══════════════════════════════════════════════════════════════╝

□ QUICK_DEPLOY.md           - 5-minute version with details
□ FREE_DEPLOYMENT_GUIDE.md  - Complete guide with alternatives
□ prepare-deployment.sh     - Run this to check if ready
□ DEPLOYMENT_SUMMARY.md     - Platform comparison & tips

╔═══════════════════════════════════════════════════════════════╗
║                    🆘 TROUBLESHOOTING                          ║
╚═══════════════════════════════════════════════════════════════╝

Backend won't start?
  → Check logs: Dashboard → Service → Logs
  → Verify all environment variables are set
  → Ensure DATABASE_URL is jdbc:postgresql://<host>:5432/<db> (no user:pass)

Frontend can't connect?
  → Check CORS_ORIGINS matches frontend URL exactly
  → Wait 30-60 seconds for backend to wake up

502 Bad Gateway?
  → Backend is sleeping, wait and refresh
  → Or check logs for startup errors

╔═══════════════════════════════════════════════════════════════╗
║                   🎯 ALTERNATIVE: RAILWAY.APP                  ║
╚═══════════════════════════════════════════════════════════════╝

Even simpler but uses $5 credit:

1. Sign up at railway.app
2. New Project → Deploy from GitHub
3. Add PostgreSQL (auto-configured)
4. Railway detects everything automatically
5. Add env vars and deploy

See FREE_DEPLOYMENT_GUIDE.md for complete Railway instructions.

╔═══════════════════════════════════════════════════════════════╗
║              ✨ CONGRATULATIONS - YOU'RE READY! ✨              ║
╚═══════════════════════════════════════════════════════════════╝

Choose your path:
  □ Fast: Follow this card
  □ Detailed: Read QUICK_DEPLOY.md
  □ Complete: Read FREE_DEPLOYMENT_GUIDE.md

Then deploy and share your app with the world! 🚀
